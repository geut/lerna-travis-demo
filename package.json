{
  "name": "@geut/lerna-travis-chan-demo",
  "version": "1.0.0",
  "description": "nothing to see here :shipit:",
  "main": "index.js",
  "scripts": {
    "test": "exit 0",
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run build --stream --parallel",
    "version": "chan release $npm_package_version --group-changes && git add CHANGELOG.md",
    "prepublish": "lerna run prepublish",
    "lerna:publish": "lerna publish prerelease --yes --npm-client yarn --preid beta --npm-tag beta"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm test"
    }
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@geut/chan": "^1.3.0",
    "husky": "^1.1.2",
    "lerna": "^3.4.3"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:geut/lerna-travis-demo.git"
  }
}
